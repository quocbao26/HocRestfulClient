<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.hocrestfulclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DanhSachSanPhamActivity" />
        <activity android:name=".ChiTietSanPhamActivity" />
        <activity android:name=".DanhSachSanPhamTheoDanhMucActivity" />
        <activity android:name=".DanhSachSanPhamTheoDonGiaActivity" />
        <activity android:name=".DanhSachDanhMucActivity" />
        <activity android:name=".ChiTietDanhMucActivity" />
        <activity android:name=".ThemSanPhamActivity" />
        <activity android:name=".ThemDanhMucActivity" />
        <activity android:name=".SuaSanPhamActivity"></activity>
    </application>

</manifest>